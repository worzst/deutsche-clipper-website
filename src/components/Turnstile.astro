---
interface Props {
  theme?: 'light' | 'dark' | 'auto';
}
const { theme = 'auto' } = Astro.props;
const siteKey = import.meta.env.PUBLIC_TURNSTILE_SITE_KEY ?? '';
---

{siteKey && <div class="cf-turnstile" data-sitekey={siteKey} data-theme={theme} />}
